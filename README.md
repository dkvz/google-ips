# Google's important IP addresses sort of lookup system

Trying to quickly find out if a Google-related IP address is actually from Google.

There's a script that downloads the most recent JSON files directly from Google.
